import React from "react";

// import { TodoState, ToggleTodoDispatchProps, Todo } from "./types";

// type TodoProps = Todo & ToggleTodoDispatchProps;

// const Todo = ({
//   id,
//   complete,
//   item
// }: // toggleCompletedStatus
// TodoProps) => {
//   return (
//     <li key={id}>
//       <button onClick={() => console.log("hey")}>
//         {item} is complete = {String(complete)}
//       </button>
//     </li>
//   );
// };

// type ListProps = TodoState & ToggleTodoDispatchProps;

// const TodoList = ({ all }: ListProps) => {
//   return (
//     <ul className="todo-list-container">
//       {all.map((todo: Todo) => {
//         const { id, item, complete } = todo;
//         return (
//           <Todo
//             key={id}
//             id={id}
//             item={item}
//             complete={complete}
//             // toggleCompletedStatus={toggleCompletedStatus}
//           />
//         );
//       })}
//     </ul>
//   );
// };

// export default TodoList;
